import React from 'react';
import BottomSheet from '../../components/BottomSheet';
// import { Container } from './styles';

const Home: React.FC = () => {
  return <BottomSheet />;
};

export default Home;
